[문제 바로가기](https://boj.kr/20492)

```c++
#include <bits/stdc++.h>

using namespace std;

int main(){
    cin.tie(NULL);
    cout.tie(NULL);
    ios_base::sync_with_stdio(false);

    int N;
    cin >> N;

    cout << (int)(N * 0.78) << " " << (int)(N * 0.956) << "\n";

    return 0;
}```